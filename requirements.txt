# Deps de PRODUTO — o que o runtime do app importa.
# É isto que o Streamlit Community Cloud instala no deploy. Manter enxuto:
# nada de scipy/scikit-learn/kagglehub aqui (são build-time do ETL, ver etl/requirements.txt).
streamlit==1.58.0
pandas==3.0.3
plotly==6.8.0
