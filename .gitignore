# Python
__pycache__/
*.py[cod]
*$py.class

# Virtual env
.venv/
venv/

# ETL — versioned, build-time pipeline (scripts + snapshots + findings).
# Snapshots committed so evaluators run without Kaggle (spec.md §13).
# Ignore only regenerated artifacts + the aggregate→profile intermediate:
etl/*.png
etl/_raw_stats.json

# Agent / IDE tooling — not project deliverables
.claude/
.antigravitycli/

# Kaggle credentials (never commit)
.kaggle/
kaggle.json

# OS
.DS_Store

# IDE
.vscode/
.idea/
